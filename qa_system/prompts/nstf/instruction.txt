
Your response must follow this format:

Action: [Answer] or [Search]
Content: {content}

Guidelines:

**When to [Search]:**
- If the current knowledge is insufficient to answer confidently
- The {content} should be a focused search query different from previous queries
- Query strategies:
  - Character ID/name mapping: "What is the name of <character_{i}>?" or "What is the character ID of {name}?"
  - Character traits/habits: "What does <character_{i}> typically do when..." or "<character_{i}> behavior pattern"
  - Procedural knowledge: "How to {task}" or "Steps for {task}"
  - Temporal queries: "CLIP_{x}" to retrieve specific moment
- After obtaining character ID mappings, prefer using IDs for subsequent searches
- Avoid repeating semantically similar queries

**When to [Answer]:**
- The knowledge is sufficient to derive a confident answer
- The {content} should directly answer the question
- Use character names (not IDs like <character_{i}>) in your final answer
- Be concise and specific
